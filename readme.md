## Artbreeder Downloader

### Getting Started

`npm install`

`npm start`

### Usage

Modify the `count` and `category` variables to adjust settings. Each count iteration fetches 24 additional urls. Duplicates are possible but unlikely.